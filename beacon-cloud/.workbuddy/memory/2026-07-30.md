# 2026-07-30

## 自我推荐材料（HR 推荐话术）
- 用户提供了一段别人的 HR 推荐反馈（王艺凡格式：个人信息/工作时间/相关经验/作品仓库），要求参照该格式写自己的版本。
- 资料来源：简历 `D:\Storage\OneDrive\桌面\简历.pdf`（杨清博，郑州电力高专软件技术，27届，20岁，男，南阳，鸿蒙国赛铜奖）+ beacon-cloud 项目。
- 简历上共两个项目：① beacon-cloud 分布式短信平台（独立负责搜索/监控/管理后台三模块）② 合家云社区物业管理平台（Spring Boot + Vue 2 前后端分离，26 接口，RBAC+JWT）。
- 产出：`E:\idea_workscope\beacon-cloud\报告\自我推荐材料-杨清博.md`（含正文 + 发送前检查清单：仓库需转 public、AI 使用口径统一为"仅提效"、工作时长待用户确认）。
- 提取简历 PDF 用 venv：`C:\Users\AQvir\.workbuddy\binaries\python\envs\default\Scripts\python.exe` + pdfplumber（本次已装入该 venv）。
- 用户追加要求突出"核心校验链路"，已重写为独立段落。核实的代码事实（写材料/面试可复用）：
  - beacon-api CheckFilterContext：6 道前置校验 apikey/ip/sign/template/mobile/fee，顺序由 `@Value("${filters:...}")` + `@RefreshScope` Nacos 热更新控制。
  - beacon-strategy StrategyFilterContext：10 个校验器（BlackClient/BlackGlobal/DirtyWordDFA×3/Fee/LimitOneHour/LimitOneMinute/Phase/Route/Transfer），**按客户维度**从 Redis hGet `client_business:{apiKey}` 的 clientFilters 字段动态编排，每客户校验链不同。
  - 两级链路都是策略模式 + Spring Map 泛型注入（beanName → filter）。
- **用户能力边界（写材料必须遵守）**：两个项目的前端（合家云 Vue 2、beacon-webmaster 页面）都是课程提供的，用户**不具备独立开发前端能力**，只能"读懂 + 按需求修改 + 联调"。严禁写"独立开发前端"。
- **格式认知纠正**：参考文本开头"王艺凡【HR面试反馈】候选人可以不借助AI独立开发VUE"那行是 **HR 面试后写的备注**，不是候选人自我介绍内容。候选人材料应直接从"一、个人信息"开始，不能替 HR 下能力结论。材料标题已改为"杨清博【自我介绍】"。
- 用户反馈材料"太多了"，已精简正文到王艺凡风格（相关经验三段、关键词堆砌不展开长段落），检查清单收敛为 3 条内部备忘。用户对"材料冗长"敏感，后续同类材料默认走简洁风。
