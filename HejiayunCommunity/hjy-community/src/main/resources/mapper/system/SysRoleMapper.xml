<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.hjycommunity.system.mapper.SysRoleMapper">

    <!-- 根据用户 id 查询用户权限 -->
    <select id="selectRolePermissionsByUserId" parameterType="long" resultType="string">
        select distinct sr.role_key
        from sys_role sr
                 left join sys_user_role sur on sur.role_id = sr.role_id
                 left join sys_user su on su.user_id = sur.user_id
        where sur.user_id = #{userId}
    </select>
</mapper>
